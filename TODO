- Bring up exclusive window
- Load configuration file?
-- --config param for config path pointing to assets, shaders, etc.
- Draw basic textured quads
- Basic carousel selector
- Keyboard navigation

- Process management solution
-- Stand up a basic daemon 
-- Port logging over to common lib
-- Daemon starts launcher
** Establish a *very* simple IPC messaging protocol:
--- Start launcher
*** Swap launcher & game (process suspend/resume/termination is implicit)
** Daemon games/processes are configurable
*** Add support for game config query messages

* Minor item: use errno for all socket failures

* Controller navigation
* Implement "selector" API via dynlib
* Hot-loadable selector implementation
* Animations and presentation
* Freetype font rendering
* Audio support
